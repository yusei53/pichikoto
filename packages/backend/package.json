{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm run build && wrangler versions upload --assets=./dist",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "typescript": "^5.0.0"
  }
}
