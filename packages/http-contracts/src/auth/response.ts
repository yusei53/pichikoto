export type VerifyTokenResponse = {
  
}

export type CallbackResponse = {
  accessToken: string;
  refreshToken: string;
}

export type RefreshTokenResponse = {
  accessToken: string;
  refreshToken: string;
}
